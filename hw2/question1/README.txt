Kernels are read ffrom a text file. Every row corresponds to a row in the kernel matrix and there must be a white space between. I also provided  in the projext  gaussian filter, a filter that sharpens and one for edge detection as examples.

Convolution Theorem takes too much time for large images. If size of an image 100x100 it takes approximately 45 seconds to complete in my pc. 640 x 480 image took literally half an hour.

